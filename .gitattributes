# Normalize line endings for all text files. Git stores them internally
# as-is but checks out CRLF on Windows, matching what Visual Studio writes.
* text=auto eol=crlf

# Never touch line endings in binary-ish or generated files.
*.png binary
*.jpg binary
*.ico binary
