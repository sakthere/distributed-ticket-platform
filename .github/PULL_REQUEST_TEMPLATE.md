## What does this PR do?

<!-- One or two sentences: what feature/fix does this add, and why. -->

## Checklist

- [ ] `dotnet build` passes with no errors
- [ ] `dotnet test` passes (new tests added if this touches Application-layer logic)
- [ ] Relevant Sprint recap doc updated (`Docs/revision/`)
- [ ] `ROADMAP.md` updated if this completes a roadmap item
- [ ] New migration included, if the Domain/Persistence schema changed
- [ ] No secrets, connection strings, or credentials committed
- [ ] Tech debt introduced by this PR is explicitly logged, not silently left undocumented

## Notes for the reviewer

<!-- Anything non-obvious: a deliberate tradeoff, a known gap, or a question you want feedback on. -->
